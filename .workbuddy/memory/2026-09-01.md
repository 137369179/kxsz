# 2026-09-01


## 凯茜识字 1:1 深度克隆（《洪恩识字》逆向拆解 → 像素级 + 机制级落地）

### 数据层
- 字库：8 字 → **200 字**（hanzi-writer-data 真实笔顺 CC BY 4.0 + pypinyin 自动拼音 + 27 条多音字订正表）
- 绘本：2 → **8 本**（含小猫钓鱼、小蝌蚪找妈妈、乌鸦喝水、司马光砸缸、三只小猪盖房等）
- 成语：2 → **32 条**（含守株待兔、画龙点睛、凿壁偷光、囊萤映雪等经典）
- 数据生成工具链：`tools/build_characters.py` / `tools/build_books.py` / `tools/build_idioms.py`，内容在 `tools/content/*.txt`

### 引擎层（1:1 还原洪恩机制）
- `utils/hanziEngine.js` 重写：Path2D 笔画轮廓 + 中线驱动引导光球 + 描红裁剪（用户笔越迹在笔画内显影）+ 倒笔画拦截 + 笔画名推断（横/竖/撇/点/横折/竖钩）+ 笔顺演示动画
- `utils/playSceneEngine.js` 新增：196 种动作归并为 7 类泛化机制（rise / wipe / tap / multi / hold / slide / drag），200 字全部覆盖
- `utils/drillEngine.js` 新增：6 类题型随机轮换（audio_choice / image_choice / similar_pick / word_fill / sentence_fill / balloon_pop），每字 3 题不重复
- `utils/ebbinghaus.js` 沿用艾宾浩斯 1-2-4-7-15-30 间隔，连对升档 / 错答回退 / 体力门禁
- `utils/soundEngine.js` 沿用 21 个 Web Audio 合成音效

### UI 层
- Tailwind CDN 改本地 vendor 化（避免 module 加载阻塞）
- 4 阶段象形演变泛化版（实物 → 图画剪影 → 古字做旧 → 现代楷书）
- 五步进度条 / Combo 评级（Good / Great / Perfect）/ 卡通勋章
- 字卡翻转 3D、家长数学门禁锁、艾宾浩斯复习入口、护眼倒计时

### 冒烟验证
- 17 个 JS 文件语法 100 通过
- Chrome headless 截图：地图主界面 / 玩 / 认 / 练 / 写 / 测（宝箱）五步全部正常渲染
- 总代码：58 821 行 JS

### 关键文件
- 字库：`src/data/characters.js`（1.08 MB，含真实笔顺）
- 内容源：`tools/content/01_nature_body.txt` / `02_number_family_action.txt` / `03_life_study_color.txt` / `04_pronoun_antonym_time.txt` / `books_extra.txt` / `idioms_extra.txt`
- 测试页：`_dev_learn_test.html?step=N` 调试各步骤

## 凯茜识字字库规模化扩容（200 → 715 字）

### 核心交付
- 字库：**200 字 → 715 字**（+515 字，3.6 倍扩容）
  - 200 字：4 个手工 9 字段批次（01-04），保留高质量故事/词组/形近字
  - 515 字：新增自动扩字器，只需 `字|类型|部首|emoji|词组` 四字段即可产出完整字库条目
- 自动扩字数据：`tools/content/auto.txt`（实物/身体/器物/场所/自然/植物/食物/动物）+ `auto2.txt`（动词/形容词/虚词/数字时间）
- 生成器增强：
  - `load_content()` 限定只读 `0数字_*.txt`，避免 books/idioms/auto 文件混读
  - 新增 `load_auto()` 读取所有 `auto*.txt`，自动填充拼音/笔顺/机制/引导语/故事模板/兜底词组
  - `STAGES_METADATA` 改为按实际字数动态生成三阶段标签（1-70 / 71-140 / 141-715，目标 1300）

### 数据质量
- 715 字全部成功获取 hanzi-writer-data 真实笔顺，失败 0
- 校验通过：无重复字、无选项异常、笔顺与中线匹配、笔画数一致
- 阶段分布：森林岛 70 / 生活岛 70 / 星际岛 575
- 机制分布：7 类手势全覆盖（rise/wipe/tap/slide/multi/hold/drag）

### 冒烟验证
- `src/` 全部 JS 语法通过
- Chrome headless 真机渲染：
  - 主界面 splash ✅
  - 715 字大地图 ✅（关卡节点、三岛标签、凯茜精灵正常）
  - 自动扩字「饭」五步学习流 ✅（玩/认/练/写/测宝箱均正常，真实笔顺描红可用）
- 服务：`http://localhost:8848`（http-server）

### 关键文件
- 生成器：`tools/build_characters.py`
- 自动扩字数据：`tools/content/auto.txt`、`tools/content/auto2.txt`
- 字库产物：`src/data/characters.js`（4.6 MB，715 字真实笔顺）
- 调试页：`_dev_learn_test.html?step=N&char=K`、`_dev_map.html`

### 下一阶方向
- 继续用 `auto*.txt` 四字段模式补到 1300 字（只需追加行，工程化成本极低）
- 优化 characters.js 体积：拆阶段懒加载或压缩笔顺坐标，降低首屏 4.6 MB 负担
- 激励机制：贴纸墙 / 勋章 / 连胜日历（现有 `progress.attendance` 已预留）

## 凯茜识字字库规模化扩容（715 → 1300 字 + 首屏瘦身）

### 核心交付
- 字库：**715 字 → 1300 字**（对齐《洪恩识字》1300 字规模，失败 0）
  - 新增字池 `tools/content/auto3.chars.txt`（纯字池，每行一字，约 776 候选），`load_auto_list()` 自动处理
  - 自动取部首：`cnradical`（`Radical(RunOption.Radical).trans_ch(ch)`，已 `pip install cnradical` 入 venv）
  - 自动配图：`RADICAL_EMOJI` 部首→emoji 映射（缺省 🔤）
  - 字形类别启发式：`PICTO_SET` / `IDEO_SET` / `COMPOUND_SET`，其余默认形声 phono
  - 词组：`WORD_MAP` 常见双字词，未命中用单字
  - 故事：走 `STORY_TEMPLATES` 模板；机制：`resolve_mechanism` 的 auto_* 规则
  - CDN 限流：fetch_stroke_data 重试 6 次 + 429 指数退避（4s·2^n）+ 本地缓存 `tools/cache/<codepoint>.json`
  - `MAX_CHARS=1300` 上限裁剪（保留手工/auto 精编，丢字池尾部低频候选 191 个）

### 首屏瘦身（笔顺数据拆懒加载）
- `build_characters.py` 不再把 `hanzi` 笔顺写进 characters.js；改为独立 `src/data/hanzi_strokes.js`（约 3.2 MB，`separators=(",",":")` 紧凑）
- 新增 `src/utils/strokeLoader.js`：`loadStrokes(char)` 动态 `import("../data/hanzi_strokes.js")` 并内存缓存；`preloadStrokes()` 预热
- `hanziEngine.js`：`new HanziEngine(canvas, charData, cb, options, strokeData)` 新增第 5 参；新增静态 `HanziEngine.create(...)` 异步工厂
- `LearnModule.stepWrite()`：进入「写」环节前 `await loadStrokes(char.char)`，再构造引擎（executor 改 async）
- 体积变化：characters.js 4.6 MB → **1.84 MB**（仅元数据）；首屏不再解析 1300 字笔顺坐标

### 数据质量（Node 校验）
- 1300 字 / 1300 笔顺条目 / 0 缺失 / 0 笔顺不匹配 / 1300 唯一
- 阶段分布：森林岛 70 / 生活岛 70 / 星际岛 1160
- 抽样 stage3「夏」：xià / 部首夊(cnradical) / 🏖️ / ideograph / mechanism=hold / 10 笔 —— Chrome 无头实测写引擎构造成功

### 冒烟验证
- JS 语法全通过；Chrome 无头：首页正常启动（title 凯茜识字）；`_dev_stroke_test.html` stage3 字写引擎 OK（10 笔）
- 服务：`http://localhost:8848`（http-server）

### 关键文件
- 生成器：`tools/build_characters.py`（含 load_auto_list / get_radical / RADICAL_EMOJI / MAX_CHARS）
- 字池：`tools/content/auto3.chars.txt`
- 字库产物：`src/data/characters.js`（1.84 MB，元数据）+ `src/data/hanzi_strokes.js`（3.2 MB，懒加载）
- 懒加载：`src/utils/strokeLoader.js`
- 调试页：`_dev_stroke_test.html`（statically 验 stage3 写引擎）

### 下一步可继续
- 激励机制（贴纸墙/勋章/连胜日历，progress.attendance 已预留）
- 字形质量精修：auto 字的 emoji 偏装饰、部分字形类别为默认形声，可逐步人工提质
- 仍可继续补字：auto3.chars.txt 尾部被裁的 191 个低频候选可另立 auto4 池（但已超 1300 上限，需先放宽 MAX_CHARS）


## 突破 1300（1490 字）+ 奖励城堡激励机制

### 字库全量启用（1490 字）
- `build_characters.py`：MAX_CHARS 改为环境变量可配（`MAX_CHARS=1300 python ...` 可复现 1300 口径；默认不设限），启用字池尾部 191 候选 → **1490 字**（仅生僻字「湲」无笔顺 404 自动剔除）
- 校验：1490 唯一 / 1490 笔顺 / 0 缺失 / 0 笔顺不匹配；阶段分布 70/70/1350；尾部抽样「饪 rèn 饣 7笔」
- RADICAL_EMOJI 补 饣/食→🍜；characters.js 2.09 MB / hanzi_strokes.js 3.80 MB

### 奖励城堡（激励机制）
- 数据层 `ebbinghaus.js`：attendance 增 `dates[]`（真实学习日 YYYY-MM-DD）；`markTodayActive()`（completeCharacter 时打卡，重算 streak，不占用 claimedToday 签到领奖位）；`seenMedals[]` + `markMedalsSeen()`
- 派生引擎 `src/utils/rewardEngine.js`：getStickers（通关贴纸+下一批剪影）/ getMedals（16 枚里程碑勋章：识字量 1→全库、复习 10/50、妙笔生花、星光、连胜 7/30、绘本 1/全、含 current/target 进度）/ getCalendar（周一起始热力月历+当前/最长连胜）/ getNewMedalIds
- UI `src/components/RewardModule.js`：贴纸墙/勋章墙/连胜日历三标签，首渲染自动"解锁亮相互礼"（礼花+toast+标记 seen）
- 路由 `app.js`：新增 reward 模式；地图 `MapModule.js` 新增第 4 个导航浮岛「🏆奖励城堡」(left-2020px)
- 无头验证 `_dev_reward_test.html`：12 项断言全 ✓（连胜 7 天、勋章 first_char+streak_7、日历今天格等）；主应用启动不受影响

### 备注
- dump-dom 提取 marker 时注意属性顺序：`id="test-out"[^>]*>` 而非 `id="test-out">`（div 带 style 属性）
- 内联 module top-level await + 动态 import 在 --dump-dom 下常显 "pending"（时序假象），验证数据加载请用静态 import

## 魔法商店（星币兑换闭环）

### 架构
- 价目 `src/data/shop.js`：SHOP_AVATARS（6 款：3 款原图免费 + 🦄200/🐼260/🐲500 emoji 头像）+ SHOP_FRAMES（5 档：经典0/黄金150/彩虹300/火焰450/水晶600）+ FRAME_CLASSES（相框→ring/shadow 样式）+ findShopItem
- 数据层 `ebbinghaus.js`：progress.shop = {owned[], equippedFrame}；isOwned（price=0 恒真）/purchase（扣币+入列，返回 {ok,reason}）/equipAvatar（profile.avatar=value，value 为图片路径或 emoji）/equipFrame
- 派生 `rewardEngine.getShopData()`：coins + avatars/frames（带 owned/equipped/affordable/frameClass）
- UI `RewardModule` 第 4 标签「🛍️魔法商店」：免费=换上/未拥有=🪙价格按钮（买不起置灰）/使用中徽标；购买成功礼花+toast，余额不足软错误提示；页脚常显当前星币
- `MapModule` 小勇士档案：头像列表改用 SHOP_AVATARS（锁定头像可直接购买，缺币提示），档案大头像应用已装备相框样式；emoji 头像渲染为 emoji 圆片而非 img；新增「奖励城堡」跳转链接

### 验证
- `_dev_shop_test.html` 16 项断言全 ✓：610→410→神龙 poor→260、owned 顺序、装备状态、免费商品拦截、重购拦截
- 主应用启动正常

### 踩坑记录（测试侧，非产品）
- 断言里模板串读 `p.coins` 是在 push 时求值，后续扣费会"污染"前序断言 → 买完立即快照再比较
