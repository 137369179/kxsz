{
  "name": "cathy-literacy",
  "version": "2.8.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp sw.js dist/sw.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "jsdom": "^30.0.1",
    "tough-cookie": "^6.0.2"
  }
}
