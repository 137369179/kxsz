# 凯茜识字 — 教学改进现状对照表

> 对照 `docs/教学效果综合改进方案.md`（v3.0，2026-09-03）与代码现状（v2.9.7，2026-09-04）  
> 用途：避免按过时文档重复排期

| 原文档项 | 原结论 | 2026-09-04 现状 | 建议 |
|---------|--------|-----------------|------|
| P0-1 FSRS 未调用 | `completeCharacter` 写固定间隔 | **已修复**：`ebbinghaus.completeCharacter` → `fsrsCompleteCharacter` | 勿再排「接上 FSRS」；关注参数调优与同日复习 |
| P0-2 评测硬编码 3 星 | Learn 结业写死满分 | **已修复**：`_evalStars` 写回 | 关注 ASR 失败降级权重，避免手动自评灌水 |
| P0-3 字库字段荒地 | 缺 meanings 等 | **部分改善**（详情惰性加载）；仍需内容扩充 | 内容生产任务，非算法任务 |
| 断点续学 | 方案要求 3 分钟续学 | **已修**：`learnProgressStore` 用 `putJSON`/`getJSON` | 保留回归测试 |
| 默认假进度 | （文档未强调） | **已修**：`charRecords` 默认 `{}` | — |
| 绘本评测发币 | （文档未强调） | **已修**：无真实分数不发币 | — |
| 转场超时改 mode | （文档未强调） | **已修**：超时只清 overlay | — |
| PK→记忆闭环 | 游戏与学习脱节 | **已改善**：错题 `recordMistake` 写错因画像 | 可再考虑 FSRS rating 写回 |
| 离线 PWA | 「完全离线」 | **叙事已修正**：核心可离线；neural TTS / ASR 需本机或权限 | SW 版本与 `APP_VERSION` 对齐为 2.9.7 |
| 上帝模块 | Learn/Play 过大 | **已拆分**：Learn → `learnSteps/*`；Play → `playHub/*` | Parent/Book 可后续再拆 |
| Review 双调度 | （对照表未记） | **已修**：`completeReview(charId, rating)` 单路径 | — |
| 年龄步骤序列 | （对照表未记） | **已修**：Learn 调用 `ebbinghausManager.getStepSequence()` | — |
| 低龄 shake | （对照表未记） | **已修**：`playSceneEngine.shakeScreen` 用真实年龄 | — |
| 每日学习时长硬限 | （对照表未记） | **已接线**：learn/review 会话计时 + 满额家长门禁延长；Parent 可设每日上限 | — |
| 清除数据 confirm | （对照表未记） | **已修**：`showConfirm` 项目内 Modal | — |
| PWA 图标 | （对照表未记） | **已修**：manifest 指向 webp/png/jpg 实文件 | 可补 192/512 标准尺寸 |
| server 路径穿越 | （对照表未记） | **已修**：拒绝含 `..` 的路径 | — |
| UI Emoji 红线 | README 宣称零 emoji | **部分清理**：Book toast / 积木屋庆祝 / Review 形象标签已去 emoji | 字库数据层 emoji 字段另议 |
| 旧文档漂移 | 改进方案仍写 FSRS 睡觉 | **已加状态横幅** 指向本对照表 | — |

## 单一版本源

- 权威：`src/utils/version.js` → `APP_VERSION`
- 同步：`package.json`、`index.html` `?v=`、`sw.js` fallback、探针页
