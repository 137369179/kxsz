# 凯茜识字 — 教学改进现状对照表

> 对照 `docs/教学效果综合改进方案.md`（v3.0，2026-09-03）与代码现状（v2.9.7，2026-09-04）  
> 用途：避免按过时文档重复排期

| 原文档项 | 原结论 | 2026-09-04 现状 | 建议 |
|---------|--------|-----------------|------|
| P0-1 FSRS 未调用 | `completeCharacter` 写固定间隔 | **已修复**：`ebbinghaus.completeCharacter` → `fsrsCompleteCharacter` | 勿再排「接上 FSRS」；关注参数调优与同日复习 |
| P0-2 评测硬编码 3 星 | Learn 结业写死满分 | **已修复**：`_evalStars` 写回；手动自评封顶 2 星防灌水 | 继续观察 ASR 失败率 |
| P0-3 字库字段荒地 | 缺 meanings 等 | **组词已补齐**；Stage1 甲骨/教学字形 **70/70**（象形符号 + 形声会意繁体占位）；全库仍大量空 | 全库扩种另排；种子 `tools/content/stage1_oracle_glyphs.json` |
| Fusion 写回灌进度 | （对照表未记） | **已修**：改走 `writeKnownCharsReview`，不灌未学字 | — |
| 上帝模块 | Learn…/app | **app.js 主壳已拆** | — |
| FSRS 年龄调优 | getDecayForAge 未接线 | **已接线**：completeCharacter/Review 传 age；低龄更短间隔 | 观察 retention 曲线 |
| 断点续学 | 方案要求 3 分钟续学 | **已修**：`learnProgressStore` 用 `putJSON`/`getJSON` | 保留回归测试 |
| 默认假进度 | （文档未强调） | **已修**：`charRecords` 默认 `{}` | — |
| 绘本评测发币 | （文档未强调） | **已修**：无真实分数不发币；第 1 关改为听音选字真测 | — |
| 转场超时改 mode | （文档未强调） | **已修**：超时只清 overlay | — |
| PK→记忆闭环 | 游戏与学习脱节 | **已改善**：错题画像 + 玩法 `completeReview`；勋章计数 `gameStats` | — |
| 离线 PWA | 「完全离线」 | **叙事已修正**：核心可离线；neural TTS / ASR 需本机或权限 | SW 与 `APP_VERSION` 对齐 2.9.7 |
| 上帝模块 | Learn/Play/Parent/Book/Map/Card/Pinyin/Reward/app | **app.js 主壳已拆**（~110 行 → appHub） | 甲骨精修 / FSRS 调优 |
| Review 双调度 | （对照表未记） | **已修**：`completeReview` 单路径 | — |
| 年龄步骤序列 | （对照表未记） | **已修**：`getStepSequence` + 家长设置年龄预览 | — |
| 低龄 shake | （对照表未记） | **已修**：真实年龄 | — |
| 每日学习时长硬限 | （对照表未记） | **已接线** + 当日去重 | — |
| 清除数据 confirm | （对照表未记） | **已修** | — |
| PWA 图标 | （对照表未记） | **已修**：192/512 | — |
| Parent 假数据 | 96%/98.4%/假勋章 | **已修**：真实报告 + `resolveTrophyUnlocks` | — |
| Review 错因键 | 字形当 charId | **已修**：`confusedTargetsForReview` | — |
| showGameToast XSS | innerHTML 全开 | **已收紧**：纯文本 textContent | — |
| server 路径穿越 | （对照表未记） | **已修**：拒绝 `..` | — |
| UI Emoji 红线 | 零 emoji | **分层**：chrome 测零；教具 emoji 保留；`stripEmoji` | 勿盲删字库 |
| 旧文档漂移 | FSRS 睡觉 | **状态横幅** 指向本表 | — |
| 版本对齐测试 | （对照表未记） | **已加** `versionAlignment` | — |
| 学习机制升级研究 | （对照表未记） | **已成文** `docs/儿童学习机制研究与功能升级方案.md` | — |
| 年龄步骤硬编码击穿 | （对照表未记） | **已修**：learnSteps 统一 `nextStep()` | 保留 auditFixRegressions |
| 跳过读默认满星 | （对照表未记） | **已修**：`_evalStars=null`，未评测结业默认 2 星 | — |
| 绘本跟读 escapeHtml / 0 分发币 | （对照表未记） | **已修**：import + 及格 60 分档发币 | — |
| 双套 PK | （对照表未记） | **已修**：`MODE_TO_MODULE.pk→play`；PKModule 仅 redirect + 单测辅助 | 业务只认 pkArena |
| PK 错选画像 | 仅 completeReview | **已修**：错选 `recordMistake(similar_confuse)` | — |
| Boss 错选画像 | 仅 completeReview | **已修**：错选同写 `recordMistake` | 超时仍只 completeReview(false) |
| Spotter 错选闭环 | 仅 recordMistake | **已修**：错选同时 `completeReview(false)` | — |
| Match 配对失败 | 仅超时写回 | **已修**：错配 `completeReview(false)` + `pronunciation` | — |
| Fusion / 绘本听音选字 | 无 FSRS 写回 | **已修**：对错均 `completeReview`；错选保留画像 | — |
| 成语/古诗/字族 | 仅星币/learned* | **已修**：`writeKnownCharsReview` 仅写回已学字 | 不灌入未学字 |
| microReview 空转 | （对照表未记） | **已修**：`microReviewUI` + 会话内 `startMicroScheduler` | — |
| SW Hub 预缓存缺失 | （对照表未记） | **已改善**：DEFAULT_CORE_ASSETS 含 appHub/Map 入口 | 生产仍用 sw-manifest |
| LEARN_FINISH 无消费者 | （对照表未记） | **已修**：≥2 星 → mascotProgress | — |

## 单一版本源

- 权威：`src/utils/version.js` → `APP_VERSION`
- 同步：`package.json`、`index.html` `?v=`、`sw.js` fallback、探针页
