# ⚠️ 严禁生成矢量 SVG 图片说明

根据用户最高规则指令：
> **“永远不要生成矢量 SVG图片，请记住这个规则，只生成精美的真实图片，如果模型有限制只能等下次再自动生成。把现在已经生成的纯矢量 SVG图片删除，重新生成。”**

- **绝对禁令**：本目录下的所有历史脚本均为过去编写的 SVG 矢量图拼装脚本，**严禁再次执行或引用**！
- **生产准则**：未来所有视觉资产（头像、拼音卡、神木、故事配图、成语、古诗插画）必须且只能通过 **真实 AI 图像生成模型**（如 `generate_image`）生成真实、高品质、具有真实质感与中国风韵的绘画/插画。
- **配额策略**：若模型遭遇 API 限制（如 429 Quota Exhausted），必须等待配额恢复后再行自动调用生成，绝不允许编写矢量 SVG 代替。
